{"id":"cth-20","title":"A provider reload mid-call never recovers the patient peer ID","notes":"Found by the cth-17 review, and beyond plan 0002's scope (its Not-in-scope lists the grace timer and the reconnect events).\n\nSection 4.4 claims 'a reconnect on either side makes a new peer and a new peer:id, so this one rule covers the first call and every later one'. That holds only when the reconnecting side re-enters ACTIVE. A provider reload does not flip the patient's active flag, so the patient makes no new peer and re-sends no peer:id; the server relays peer:id without latching it, so the reloaded provider never learns the patient's ID and the call cannot be re-established.\n\nFix shape, for whoever picks up the reconnect/grace plan: either have a client re-send its peer ID when it sees the other side's presence go from false to true, or have the server hold the last peer ID per role and replay it on join - which section 4.3 currently forbids, so that route needs the contract changed first.\n\nREADME now records this and the server-restart case as deliberately left out, under 'Deliberately left out'. The card stays open: the note documents the gap, it does not close it.\n\nDiscussed 2026-09-13: confirmed this needs presence.provider to flip false->true on reload, which requires provider-disconnect handling. Architecture §3 ties any provider disconnect to the full DISCONNECTED_GRACE transition (not yet built), so a presence-only shortcut would diverge from the documented design. Decision: leave this blocked on the reconnect/grace plan rather than build a partial/stopgap presence mechanism now.","category":"client","tags":["media","reconnect"],"status":"backlog","rank":"t","created":"2026-09-13T03:10:01.243Z","updated":"2026-09-13T08:59:28.464Z","refs":["docs/architecture.md"],"blockedBy":[]}
{"id":"cth-1","title":"Create packages/shared with the Zod contract from section 4","notes":"New workspace @coviu/shared. Root package.json workspaces glob is already packages/*, so no change there. zod is NOT installed yet - add it here.\n\nOne schema per HTTP body/response (§4.2), one per Socket.IO payload (§4.3), plus the SessionStatus union (§3), ended_reason union, event type union (§5.2) and ErrorCode union (§4.5). Every exported type via z.infer - no hand-written interface may duplicate a schema. Also export ClientToServerEvents and ServerToClientEvents built from those inferred types.\n\nTrap: writing these is where §4 gets tested against itself. If a payload in the doc is underspecified, fix docs/architecture.md in the same change - a doc/code disagreement is a defect in one of them (top of that file). Do not paper over it downstream.\n\nDone when: both other packages can import @coviu/shared and a wrong Socket.IO event name fails tsc, before any runtime schema runs.\n\nBuilt @coviu/shared as source-only (package.json main/types/exports all point at src/index.ts, no build step) so tsx and Vite's bundler resolution pick it up directly. Files: status.ts (SessionStatus/EndedReason/Role), errors.ts (ErrorCode/ErrorPayload), events.ts (EventType + per-type data schemas + Event), http.ts (the three §4.2 endpoints, including the events endpoint even though it's out of scope for the current plan slice - the shared contract covers the whole doc), socket.ts (all §4.3 payloads plus ClientToServerEvents/ServerToClientEvents). Verified the 'wrong event name fails tsc' requirement with a throwaway smoke file in packages/server before deleting it. Added @coviu/shared as a real dependency in server and client package.json (needed for the import to resolve under NodeNext/bundler resolution) - not just documentation, actual wiring. Deleted the packages/shared line from architecture.md §8 and narrowed the remaining dependency-gap bullet to the still-missing ones.","category":"shared","tags":["contract"],"status":"done","rank":"V","created":"2026-09-12T04:09:16.768Z","updated":"2026-09-12T04:15:26.526Z","refs":["docs/plans/0001-contract-persistence-skeleton.md","docs/architecture.md","packages/shared/src/index.ts","packages/shared/package.json"],"blockedBy":[]}
{"id":"cth-2","title":"Add the SQLite schema for sessions and events","notes":"Tables exactly as §5.1 and §5.2: sessions (with the ended_reason CHECK constraint) and events (AUTOINCREMENT id, JSON text data column) plus idx_events_session_id. Created on start-up if absent, at data/app.db.\n\nDecision from the plan: use node:sqlite, not better-sqlite3. This machine is Node 26.6 where node:sqlite is stable, and §2.1 says prefer it. Remove better-sqlite3 from packages/server/package.json and add engines: { node: '>=22.5' }.\n\nDone when: a fresh checkout with no data/ directory starts and 'sqlite3 data/app.db .schema' prints both tables, the CHECK, and the index.\n\nImplemented in packages/server/src/persistence/schema.ts (applySchema: CREATE TABLE IF NOT EXISTS for sessions/events + idx_events_session_id, per §5.1/§5.2) and database.ts (openDatabase: mkdir data dir if absent, opens node:sqlite DatabaseSync, runs applySchema). index.ts calls openDatabase('data/app.db') on startup. Removed better-sqlite3 from packages/server/package.json, added engines >=22.5, ran npm install to drop it from the lockfile. Added data/ to .gitignore. Tests: schema.test.ts and database.test.ts against in-memory / tmp-dir databases, covering column shape, the CHECK constraint (rejects bad reason, accepts the three valid ones, allows NULL), the index, idempotent re-apply, and directory creation on a fresh path. Verified manually: rm -rf data && npx tsx packages/server/src/index.ts && sqlite3 data/app.db .schema prints both tables, the CHECK, and the index - matches the done criterion exactly. Repository functions (upsertSession etc, cth-3) are NOT implemented here - out of scope for this task.","category":"persistence","tags":["db"],"status":"done","rank":"l","created":"2026-09-12T04:09:16.824Z","updated":"2026-09-12T04:50:55.679Z","refs":["docs/plans/0001-contract-persistence-skeleton.md","docs/architecture.md","packages/server/package.json"],"blockedBy":[]}
{"id":"cth-3","title":"Implement the six repository functions over SQLite","notes":"§5.3, exactly six exports and nothing more: upsertSession, getSession, getSessionByKey, recordEvent, getEvents, listOpenSessions. This is the ONLY module in the tree allowed to write SQL or import the sqlite driver (§6.2).\n\nContract details the doc implies but does not spell out: getSession/getSessionByKey return undefined for no match (not null, not a throw); getEvents parses data from JSON text and yields null where the column is null; getEvents orders by id ascending; recordEvent against a missing session_id must fail rather than write an orphan - enable foreign keys.\n\nupsertSession is deliberately one function for both create and transition (§5.3) - do not split it.\n\nDone when: unit tests against an in-memory database cover all six, and getEvents returns rows oldest-first with data already parsed.\n\nImplemented in packages/server/src/persistence/repository.ts: createSqliteRepository(db) returns the six functions (upsertSession, getSession, getSessionByKey, recordEvent, getEvents, listOpenSessions) as one object, so the module's only export is that factory plus the Repository type — kept as a factory (rather than bare module-level functions against a hidden singleton db) so tests can point it at a fresh in-memory database each time, per the plan's testing approach. Session row shape lives in the new persistence/types.ts (camelCase Session, built from @coviu/shared's SessionStatus/EndedReason unions).\n\nEnabled 'PRAGMA foreign_keys = ON' inside applySchema (schema.ts) rather than at the call site, so every caller of applySchema - production database.ts and every test - gets FK enforcement automatically; verified recordEvent against a missing session_id throws 'FOREIGN KEY constraint failed'.\n\ngetSession/getSessionByKey return undefined (row ?? undefined pattern), not null. getEvents orders 'ORDER BY id ASC' and JSON.parses the data column, yielding null when the column is null. upsertSession is one INSERT ... ON CONFLICT(id) DO UPDATE, not split into create/update paths.\n\n32 tests total (23 in this task's own repository.test.ts) pass; tsc and biome clean. Manually verified via tsx against both an in-memory and the real openDatabase() path.","category":"persistence","tags":["db"],"status":"done","rank":"t","created":"2026-09-12T04:09:16.865Z","updated":"2026-09-12T05:27:41.937Z","refs":["docs/plans/0001-contract-persistence-skeleton.md","docs/architecture.md"],"blockedBy":[]}
{"id":"cth-4","title":"Build the Session registry and its emitter for CREATED and WAITING","notes":"§6.2 Session module and the §3 state machine, but only the two states this slice needs: CREATED on create, WAITING when the patient socket connects. No grace timer, no ACTIVE, no ENDED transitions beyond what the start-up sweep needs.\n\nHard rule: Session imports persistence never, holds no keys, reads no chat. It owns an in-memory registry keyed by sessionId and a typed emitter that fires on each transition and each presence change.\n\nDone when: every unit test for this module runs with no socket, no HTTP and no database, and a transition emits exactly once.\n\nImplemented as SessionRegistry (packages/server/src/session/sessionRegistry.ts): in-memory Map<sessionId, {status, presence}>, no persistence import. createSession() adds a CREATED entry with no emission (Services.createSession writes the DB row itself, per architecture §6.2 exception). patientConnected() is the only transition in scope: CREATED->WAITING, sets presence.patient=true, emits one 'transition' event {sessionId, from, to, eventType: 'patient_joined_waiting_room', presence} via a typed EventEmitter (declaration-merge pattern for on/emit). Throws if session unknown or not in CREATED. 7 unit tests, no socket/HTTP/DB.","category":"server","tags":["state-machine"],"status":"done","rank":"x","created":"2026-09-12T04:09:16.905Z","updated":"2026-09-12T10:31:55.252Z","refs":["docs/plans/0001-contract-persistence-skeleton.md","docs/architecture.md","packages/server/src/session/sessionRegistry.ts","packages/server/src/session/sessionRegistry.test.ts"],"blockedBy":[]}
{"id":"cth-5","title":"Subscribe the event recorder to the Session emitter","notes":"§6.2 'Event recorder'. A subscriber, not a caller: it attaches to the Session emitter at start-up and turns each emission into a recordEvent call, and each transition also into an upsertSession call.\n\nThe point of this task is that the state machine never writes to the database. The one exception is createSession, which writes the first sessions row itself because only that path holds the keys.\n\nDone when: driving a Session transition in a test with no HTTP layer present leaves the expected events row and updated sessions row behind.\n\nImplemented as attachEventRecorder(registry, repository) in packages/server/src/eventRecorder/eventRecorder.ts. Subscribes to SessionRegistry's 'transition' event; on each one calls repository.recordEvent(sessionId, eventType), then repository.getSession(sessionId) to fetch the existing row (written earlier by Services.createSession, which holds the keys) and repository.upsertSession({...session, status: to}) to persist the new status without ever knowing the keys itself. Test drives registry.patientConnected() against an in-memory SQLite repository (no HTTP/socket) and asserts both the new events row and the updated sessions row.","category":"server","tags":["persistence"],"status":"done","rank":"z","created":"2026-09-12T04:09:16.948Z","updated":"2026-09-12T10:32:04.477Z","refs":["docs/plans/0001-contract-persistence-skeleton.md","docs/architecture.md","packages/server/src/eventRecorder/eventRecorder.ts","packages/server/src/eventRecorder/eventRecorder.test.ts"],"blockedBy":[]}
{"id":"cth-6","title":"Add Services: createSession, resolveKey, endInterruptedSessions","notes":"§6.2 Services, three of the five functions - getSessionEvents and getChatHistory are out of scope for this slice.\n\ncreateSession: node:crypto randomUUID for the id and randomBytes for the two keys (§2.1), writes the first sessions row and the session_created event, adds to the registry, returns the two links. The response must not contain sessionId (§4.1, §4.2).\nresolveKey: returns { sessionId, role, status } or nothing. The role derivation in §4.1 lives in this one function and nowhere else - the caller never declares its role.\nendInterruptedSessions: the §3 start-up sweep. Every non-ENDED row moves to ENDED reason 'interrupted' with the event written. It must run before the server accepts a request, and must not touch the registry (empty at that moment).\n\nDone when: restarting the server turns a previously ACTIVE row into ENDED/interrupted and a later request with that key gets session_ended.\n\nImplemented in packages/server/src/services/services.ts. createSession/resolveKey/endInterruptedSessions per §6.2. baseUrl for providerUrl/patientUrl is injected into createServices (not read per-request), sourced from CLIENT_URL env var in index.ts, default http://localhost:5173. Links use /p/:providerKey and /w/:patientKey per shared http.test.ts convention. endInterruptedSessions writes recordEvent+upsertSession directly (bypasses registry/event-recorder, as required).","category":"server","tags":["services"],"status":"done","rank":"zV","created":"2026-09-12T04:09:16.990Z","updated":"2026-09-12T11:35:23.021Z","refs":["docs/plans/0001-contract-persistence-skeleton.md","docs/architecture.md"],"blockedBy":[]}
{"id":"cth-7","title":"Serve POST /api/sessions and GET /api/sessions/:key on one http.Server","notes":"§4.2, first two rows only - the events endpoint is out of scope. Express 5 attaches to the same http.Server Socket.IO will use: one listener, one port (§2.1).\n\nEvery path param and body parses through a shared schema; no handler reads a raw param (§4.2). Parse failure returns { code: 'invalid_message', message } at 400; no session for that key returns unknown_key at 404 (§4.5). Deliberately the same code for a missing key and for a patientKey on a provider-only route - a distinct code would leak that the session exists.\n\nHTTP holds no state machine code and no SQL - it calls Services or Session only.\n\nDone when: curl POST /api/sessions returns the four fields and no sessionId, and GET with a bad key returns 404 { code: 'unknown_key' }.\n\nImplemented in packages/server/src/http/http.ts: createHttpApp(services) returns an Express app, wired up in index.ts on one http.Server. Both routes parse via shared Zod schemas (safeParse, with req.body ?? {} to handle no-body POST). Verified with curl: POST /api/sessions returns the four fields and no sessionId; GET with a bad key returns 404 {code:'unknown_key'}.","category":"server","tags":["http"],"status":"done","rank":"zl","created":"2026-09-12T04:09:17.032Z","updated":"2026-09-12T11:35:23.059Z","refs":["docs/plans/0001-contract-persistence-skeleton.md","docs/architecture.md"],"blockedBy":[]}
{"id":"cth-8","title":"Wire the Socket.IO handshake middleware, room join, and first session:state","notes":"§4.3. This is the server half of 'client connects and says hello'. There is no hello event and this task must not add one - the handshake carries the key and the first session:state is the reply.\n\nio.use middleware: read auth.key, call resolveKey, attach sessionId and role to the socket, join room session:<sessionId>. Refuse with unknown_key (no match), role_already_connected (that role already has a socket), or session_ended (state ENDED). Emit session:state to the socket on connect.\n\nConfig: pingInterval 10s, pingTimeout 20s. Do NOT enable connectionStateRecovery (§4.3 says why). Write no heartbeat code.\n\nHard rule: Signaling never imports persistence. It maps events to Services or Session calls and decides nothing about state.\n\nDone when: a socket.io-client with a valid key connects and receives session:state; with a junk key it gets connect_error carrying { code: 'unknown_key' }.\n\nImplemented in packages/server/src/signaling/signaling.ts: attachSignaling(io, services, registry). io.use handshake middleware reads auth.key, calls services.resolveKey, refuses unknown_key/session_ended/role_already_connected (the last tracked via a local Map<sessionId, {provider?,patient?}> of socket ids — transport bookkeeping only, not persisted, since Session/registry holds no socket refs). On connection: joins room session:<sessionId>, calls registry.patientConnected() when a patient connects to a CREATED session (drives the CREATED->WAITING transition already in sessionRegistry.ts), emits session:state to the connecting socket, and registry.on('transition') broadcasts session:state to the room for later transitions. 'since' is computed as the emission timestamp (registry does not track transition timestamps yet - out of scope here). Wired into index.ts with pingInterval 10s/pingTimeout 20s, connectionStateRecovery left off. Added socket.io-client as a server devDependency for tests. Verified with a live tsx smoke test: valid key -> session:state; junk key -> connect_error {code:'unknown_key'}.","category":"server","tags":["signaling"],"status":"done","rank":"zt","created":"2026-09-12T04:09:17.074Z","updated":"2026-09-12T11:46:34.921Z","refs":["docs/plans/0001-contract-persistence-skeleton.md","docs/architecture.md"],"blockedBy":[]}
{"id":"cth-10","title":"Widen vitest include to .tsx and confirm the test convention","notes":"vitest.config.ts line 5 is include: ['packages/*/src/**/*.test.ts'] - it matches no .tsx, so client tests written per §6.4 would silently not run. Widen to *.test.{ts,tsx}.\n\n§6.4: tests sit next to the code they test. Do not add a __tests__ directory. The top-level tests/ project for e2e is out of scope for this slice.\n\nDone when: a deliberately failing .test.tsx under packages/client/src is picked up by npm test.\n\nWidened include to *.test.{ts,tsx} in vitest.config.ts. Verified with a scratch .test.tsx under packages/client/src that deliberately failed and was picked up by vitest run, then removed it.","category":"tooling","tags":["tests"],"status":"done","rank":"zx","created":"2026-09-12T04:09:17.161Z","updated":"2026-09-12T12:07:20.988Z","refs":["docs/plans/0001-contract-persistence-skeleton.md","vitest.config.ts","docs/architecture.md"],"blockedBy":[]}
{"id":"cth-9","title":"Convert the client to React and wire the session/socket layer","notes":"packages/client/src/main.ts is currently one innerHTML line. Install react, react-dom, @vitejs/plugin-react and socket.io-client (none are installed). Make packages/client/tsconfig.json extend ../../tsconfig.base.json and set jsx: react-jsx - it extends nothing today.\n\nSession/socket layer per §6.3: knows nothing about either view, wraps the §4.2 HTTP calls and the Socket.IO client, turns socket events into React state, uses the shared types. One socket for the whole app. It holds NO reconnect or backoff code - socket.io-client does that. A middleware refusal is not retried, so on connect_error show a terminal screen; do not add a retry on top.\n\nKey goes in the handshake - io(URL, { auth: { key } }) - never in a URL (§4.3: URLs reach history, Referer and access logs).\n\nProvider and patient views are separate top-level components, not one component with a role condition (§6.3). Minimal UI: role, state, error. No media layer, not even a stub.\n\nDone when: two tabs on the provider and patient links each render the state they received over the socket.\n\nImplemented: react/react-dom/@vitejs/plugin-react/socket.io-client added to packages/client; vite.config.ts added with the React plugin; tsconfig.json now extends ../../tsconfig.base.json with jsx: react-jsx. Session/socket layer is session/httpClient.ts (wraps POST /api/sessions and GET /api/sessions/:key, throws HttpError carrying the parsed ErrorPayload) plus session/useSession.ts, a hook that resolves the key over HTTP, opens the one socket.io-client connection with auth:{key}, and turns session:state/error/connect_error into a small SessionConnection union (resolving/error/connected). No retry/backoff code; a connect_error is terminal per §4.3, matching the middleware-refusal-is-not-retried rule. Provider and Patient are separate top-level components (ProviderView.tsx, PatientView.tsx) rendering role/state/presence/error only, no media layer. HomeView.tsx creates a session and shows the two links. App.tsx does plain-regex path matching on /p/:key and /w/:key (no router dependency - not in the §2.1 table) and falls back to HomeView. main.tsx mounts App via createRoot.\n\nVerified end-to-end against the actually-running server (not just unit tests): a raw socket.io-client script (provider socket + patient socket) showed session:state CREATED->WAITING delivered to both connections, matching what ProviderView/PatientView render. Could not get a real browser screenshot - this sandbox has no network access to download a Playwright/Chromium binary - so the DOM-rendering side relies on the passing @testing-library/react + jsdom tests (5 view/App test files, all green) plus this raw-socket integration check.\n\nFound and filed as a separate task, not fixed here (out of scope - cth-8 explicitly scoped itself to handshake/room/first session:state only): signaling.ts has no admit/end-session/patient:leave handlers and never sets presence.provider true, so the Admit/End session buttons in ProviderView currently have no server-side effect. See cth-11.","category":"client","tags":["react","socket"],"status":"done","rank":"zz","created":"2026-09-12T04:09:17.116Z","updated":"2026-09-12T12:13:59.364Z","refs":["docs/plans/0001-contract-persistence-skeleton.md","docs/architecture.md","packages/client/src/App.tsx","packages/client/src/session/useSession.ts","packages/client/src/session/httpClient.ts","packages/client/src/views/ProviderView.tsx","packages/client/src/views/PatientView.tsx","packages/client/src/views/HomeView.tsx","packages/client/vite.config.ts","packages/client/tsconfig.json"],"blockedBy":[]}
{"id":"cth-12","title":"Add CORS to Express and Socket.IO for the client origin","notes":"Reported by user: HomeView's Create session button hit a CORS error against the server. Root cause: createHttpApp had no cors middleware, and the Socket.IO Server was constructed with no cors option - architecture.md never calls this out since it doesn't discuss deployment topology, but client (5173) and server (3001) are different origins in dev. Fix: cors({ origin: clientUrl }) on the Express app (clientUrl now a required second param to createHttpApp) and cors: { origin: CLIENT_URL } on the Socket.IO Server, both driven by the existing CLIENT_URL env var. Verified with a live curl against Origin: http://localhost:5173 showing Access-Control-Allow-Origin echoed back.","category":"server","tags":["bug","cors"],"status":"done","rank":"zzV","created":"2026-09-12T12:21:13.027Z","updated":"2026-09-12T12:21:19.083Z","refs":["packages/server/src/http/http.ts","packages/server/src/index.ts"],"blockedBy":[]}
{"id":"cth-11","title":"Wire admit, end-session, patient:leave handlers and provider presence in Signaling","notes":"Found while manually verifying cth-9 (client) end-to-end against the real running server with a raw socket.io-client script. Observed: (1) signaling.ts only sets presence.patient on patient connect when status is CREATED - there is no code path that sets presence.provider true on provider connect. (2) There are no socket.on('admit'|'end-session'|'patient:leave', ...) handlers registered at all, so those client emits are silently dropped and no session:state transition (WAITING->ACTIVE, ->ENDED) ever fires. Per architecture.md §4.3 table, the server must accept these events per-state, call the matching Session command, and reject with not_allowed_in_state/session_ended/role checks. This blocks the provider Admit/End session buttons added in cth-9 from having any visible effect, though it does not block cth-9's own done condition (both tabs still render the WAITING state they receive over the socket).\n\nImplemented:\n\nsessionRegistry.ts: added providerConnected (idempotent presence.provider=true, no status change, emits a new 'presence' event rather than 'transition' since there is no persisted event type for a first provider connect), admit (WAITING->ACTIVE, eventType patient_admitted), endSession (any-but-ENDED->ENDED, eventType session_ended, hardcoded reason provider_ended since that's the only caller in scope - the timeout/interrupted reasons go through separate paths that don't touch the registry), and patientLeft (presence.patient=false, from===to, eventType patient_left - the session does not end per §4.3). SessionTransitionEvent gained an optional endedReason field.\n\neventRecorder.ts: now forwards endedReason into recordEvent's data ({reason} for session_ended) and sets endedAt/endedReason on the persisted sessions row when a transition's 'to' is ENDED - previously only status was updated, so a real end-session would have left ended_at/ended_reason null despite status=ENDED.\n\nsignaling.ts: registered socket.on for admit/end-session/patient:leave, each: parses the (empty-object) payload through its shared Zod schema, then checkLegal() enforces the §4.3 role check (provider-only for admit/end-session, patient-only for patient:leave) and the per-state legal-message table, sending 'error' with not_allowed_in_state or session_ended as appropriate before ever calling the registry command. Added a registry.on('presence', ...) broadcaster alongside the existing 'transition' one, and call registry.providerConnected() on provider connect. Also fixed session:state's reason field, which was hardcoded null - it now carries endedReason when to===ENDED, per §4.3's 'reason has a value only in state ENDED, and holds the same value as sessions.ended_reason'.\n\nDeliberately still out of scope (would need the grace-timer subsystem, a materially larger piece of work not implied by this task's title): DISCONNECTED_GRACE, the reconnect-in-grace-period path, provider_disconnected/provider_reconnected/patient_disconnected/patient_reconnected events, session_timed_out. chat:message and peer:id relaying are also untouched - no handlers registered for them yet.\n\nVerified two ways: (1) full test suite - sessionRegistry.test.ts (+16 tests), eventRecorder.test.ts (+1), signaling.test.ts (+9, plus fixed one stale expectation that assumed presence.provider stayed false) - 115 total tests passing repo-wide. (2) live end-to-end against the actually-running dev server with a raw socket.io-client script: provider connect -> presence.provider true broadcast to the room; patient admit rejected not_allowed_in_state; provider admit -> ACTIVE broadcast to both; patient:leave -> presence.patient false, still ACTIVE; provider end-session -> ENDED reason provider_ended broadcast to both. Confirmed via sqlite3 that data/app.db recorded the exact event sequence (session_created, patient_joined_waiting_room, patient_admitted, patient_left, session_ended{reason:provider_ended}) and the sessions row landed with status=ENDED, ended_reason=provider_ended, ended_at set.\n\nImplemented: SessionRegistry gained admit (WAITING->ACTIVE), endSession (any-but-ENDED->ENDED, reason provider_ended), patientLeft (presence.patient=false, no status change, still records patient_left), and providerConnected (idempotent, presence.provider=true, no persisted event - none exists in the §5.2 table for a first provider connect, so it emits a new registry 'presence' event rather than 'transition'). eventRecorder now reads endedReason off the transition event to set sessions.ended_at/ended_reason and to write session_ended's {reason} data - previously it always wrote status only, so an ENDED row's ended_at/ended_reason would have stayed NULL forever.\n\nSignaling now registers admit/end-session/patient:leave handlers. Each: parses the payload with its shared Zod schema (invalid_message on failure), checks role (not_allowed_in_state if wrong), checks the session isn't ENDED (session_ended) and is in a state the architecture.md §4.3 table allows for that message (not_allowed_in_state otherwise), then calls the matching registry command. Also broadcasts session:state's reason field with the real ended_reason instead of always null, and wires registry's new 'presence' event to broadcast session:state on a presence-only change (i.e. provider connecting), which the old code never did - the provider's own presence was simply never tracked.\n\nVerified against the real running server end-to-end (not just unit tests) with a raw socket.io-client script: patient-attempted admit rejected, provider admit -> ACTIVE broadcast to both, patient:leave flips presence without ending the session, end-session -> ENDED with reason provider_ended broadcast to both, a message after ENDED gets session_ended. Confirmed via sqlite3 that the sessions row got ended_at/ended_reason set and the events table has the full expected sequence (session_created, patient_joined_waiting_room, patient_admitted, patient_left, session_ended with {\"reason\":\"provider_ended\"}).\n\nDeliberately still out of scope, per the original finding: chat:message/peer:id relay, and the DISCONNECTED_GRACE grace-timer/reconnect machinery (§3). DISCONNECTED_GRACE is listed in the per-state message table and handled defensively (patient:leave and end-session both permit it) but nothing in the codebase can currently produce that state.","category":"server","tags":["session","signaling"],"status":"done","rank":"zzl","created":"2026-09-12T12:13:26.164Z","updated":"2026-09-12T12:53:28.655Z","refs":["packages/server/src/signaling/signaling.ts","packages/server/src/session/sessionRegistry.ts","packages/server/src/eventRecorder/eventRecorder.ts","docs/architecture.md"],"blockedBy":[]}
{"id":"cth-13","title":"Mount ExpressPeerServer on the existing http.Server at /peerjs","notes":"§6.2 says the PeerServer is not a module: the composition root mounts it and passes it\nno handler. So this is index.ts only, no new file. peer@1.0.2 is already installed and exports\nExpressPeerServer and PeerServer.\n\nMount on the same http.Server that Express and Socket.IO already use (index.ts:26-33) — one\nprocess, one port. Leave allow_discovery at its default false.\n\nExpect CORS trouble: the PeerJS client hits /peerjs over HTTP before it upgrades to a WebSocket,\nand the client is on :5173 while the server is on :3001. app.use(cors({origin: clientUrl})) is\nregistered first in http.ts:16, so mounting on the same app should inherit it. cth-12 was the same\nclass of bug. The failure mode is a silent Peer that never fires 'open', so confirm in a browser.\n\nDone when: a browser-side Peer pointed at this server reaches 'open' with a random ID, and no\nprocess or port was added.\n\nMounted ExpressPeerServer on the shared http.Server at app.use('/peerjs', ...) in index.ts with allow_discovery:false and corsOptions.origin:CLIENT_URL; lint/tsc/tests all pass (2 pre-existing unrelated lint errors in package.json and .tasks/config.json, verified present on master before this change).\n\nVerified live: GET /peerjs/peerjs/id returns a random UUID with a single Access-Control-Allow-Origin: http://localhost:5173 header. Mounted at /peerjs with peer's default path / and key peerjs, so a browser Peer needs path: '/peerjs'. corsOptions narrowed from peer's default {origin:true} to CLIENT_URL. Controller decision: no unit test for this card because index.ts is the composition root and a test would have to rebuild the whole process; cost if wrong is that a future edit could unmount /peerjs with no test failing, which the client cards would catch immediately. Commits ccfc529..41dddb6.","category":"server","tags":["media","peerjs"],"status":"done","rank":"zzt","created":"2026-09-13T01:09:20.258Z","updated":"2026-09-13T02:25:30.422Z","refs":["docs/plans/0002-peerjs-media.md","packages/server/src/index.ts"],"blockedBy":[]}
{"id":"cth-14","title":"Relay peer:id between the two sockets in ACTIVE","notes":"PeerIdPayloadSchema already exists in shared (socket.ts:21) with no importer. Add the\nhandler.\n\nTwo things differ from the three existing handlers. It is legal for BOTH roles, so checkLegal's\nrequired-role argument has to become optional rather than being copy-pasted around — widen it, do\nnot add a second state check beside it. And it is a relay, not a registry command: use\nsocket.to(roomName(sessionId)).emit('peer:id', payload) so the sender is excluded and Signaling\nstill keeps no map of peer sockets.\n\nStore nothing. No registry field, no events row — §4.3 says the server keeps no record of a peer\nID. Signaling still never imports persistence.\n\nAllowed in ACTIVE only (§4.3 table). Done when: a peer:id from either role in ACTIVE arrives at\nthe other socket and not the sender; the same message in WAITING returns not_allowed_in_state, in\nENDED returns session_ended, and a malformed payload returns invalid_message and is dropped.\n\nRelayed peer:id between the two sockets in ACTIVE only, via a widened checkLegal(requiredRole: Role | null); no storage, socket.to excludes the sender. 6 new tests (relay both directions, WAITING/ENDED rejection, malformed payload, no events row) plus 115 pre-existing all pass; lint and tsc clean. Commit d596a1a.\n\nRelay landed in 553a267..ab53194. Review round 1 found two minor test gaps (no proof unknown payload keys are stripped by emitting parsed.data; the ENDED case did not assert the other socket got nothing); both fixed in ab53194. checkLegal widened to Role | null rather than duplicated, three existing call sites unchanged. 122 tests pass, lint and tsc clean.","category":"server","tags":["media","signaling"],"status":"done","rank":"zzx","created":"2026-09-13T01:09:20.305Z","updated":"2026-09-13T02:35:00.282Z","refs":["packages/server/src/signaling/signaling.ts","packages/server/src/signaling/signaling.test.ts"],"blockedBy":[]}
{"id":"cth-15","title":"Build the client media layer over the PeerJS Peer","notes":"New directory packages/client/src/media/. The one piece of this slice with no sibling to\ncopy, and the step most likely to force a rethink in the plan.\n\nIt wraps peerjs only: local capture, the peer lifecycle, and the call. It exposes the local stream,\nthe remote stream, the local peer ID once 'open' fired, and a 'call this peer ID' method. §6.3:\nit NEVER imports the socket layer and never emits a socket event. One import in that direction\ncollapses the boundary.\n\nTake the Peer factory and getUserMedia as arguments rather than reaching for the globals — jsdom\nhas neither, and a hook that calls them directly can only be tested by mocking the module. That\ninjection is what makes the tests possible at all, so write them first.\n\nconfig.iceServers = a public STUN url (stun:stun.l.google.com:19302 per §4.4). Hardcode it; the\nconfig endpoint is §7 and out of scope.\n\nTeardown must stop every track and destroy the peer. Do NOT destroy the peer on a socket drop —\n§3 says the two connections fail independently.\n\nDone when: against a fake Peer factory and a fake getUserMedia, tests show a peer ID surfacing\nafter 'open', an outbound call carrying the local stream, an answered inbound call, a remote\nstream surfacing on 'stream', and teardown stopping every track.\n\nMedia layer landed in packages/client/src/media (useMedia + peerFactory), 19 tests; createBrowserPeer uses new Peer(options) since new Peer(undefined, options) does not typecheck\n\nRound 2: effect deps down to [active] via refs, capture vs connection errors split, one-call-per-activation guard (callPlacedRef), teardown order and handler gating pinned by tests; 25 media tests\n\nLanded in ab53194..354f7c8. Review round 1 found one blocker: the effect depended on the injected createPeer/getUserMedia identities, so an inline arrow in a view would destroy the peer and re-prompt for the camera every render. Fixed by holding both in refs, leaving [active] as the only dependency - so cth-17 may pass inline arrows safely. Also fixed: capture-vs-connection error strings were conflated by a trailing .catch; callPlacedRef now guards both directions so a side that answered an inbound call never also places an outbound one, and a second inbound call is closed rather than answered; all handlers gated on the cancelled flag. Controller decision: callPlacedRef is cleared only on teardown, so a dropped call is not re-established within one ACTIVE period - recovery belongs to the reconnect plan; cost if wrong is that a mid-call network blip needs an End session and a new session to recover. 25 media tests, 147 total.","category":"client","tags":["media","peerjs"],"status":"done","rank":"zzz","created":"2026-09-13T01:09:40.688Z","updated":"2026-09-13T02:51:43.915Z","refs":["packages/client/src/media/useMedia.ts","packages/client/src/media/peerFactory.ts"],"blockedBy":[]}
{"id":"cth-16","title":"Add sendPeerId and a latched remote peer ID to useSession","notes":"useSession today exposes admit, endSession, leave (useSession.ts:78-82) and listens for\nsession:state and error only. Add an emit for peer:id and a listener that LATCHES the received ID\ninto React state.\n\nLatching is the whole point, not an implementation detail. The server replays nothing, so if the\npatient's peer opens before the provider's — which on a fast machine is most of the time — a\nuseEffect that calls on receipt of peer:id silently never calls. Holding the ID as state means the\nview re-renders when either side becomes ready and the call fires on the render where both are\ntrue.\n\nDone when: a test emitting peer:id from the fake server before the local peer is ready still\nleaves the received ID readable from the hook, and sendPeerId puts the payload on the wire.\n\nWidened useSession with remotePeerId (latched state) and sendPeerId; tsc -p packages/client now fails in 3 out-of-scope view/App test files whose UseSessionResult mock literals lack the two new fields — expected to be fixed by the view-wiring card.\n\nLanded in 354f7c8..0b1db4b. remotePeerId is React state set from a new peer:id listener and reset when baseUrl/key change; sendPeerId emits through the same optional-chained socketRef as the other three. Review round 1: the interface widening broke tsc in three view test fixtures (fixed in acf2e80). Round 2: all four returned emitters are now useCallback([]) memoised, because cth-17 puts sendPeerId in a useEffect dependency array and unstable identities would re-emit peer:id on every render. Decision: no client-side PeerIdPayloadSchema check, matching the other two listeners which also do not validate; cost if wrong is a malformed peerId reaching peer.call, which the server already rejects before relaying. 153 tests pass.","category":"client","tags":["media","socket"],"status":"done","rank":"zzzV","created":"2026-09-13T01:09:40.734Z","updated":"2026-09-13T02:57:15.302Z","refs":["docs/plans/0002-peerjs-media.md","packages/client/src/session/useSession.ts"],"blockedBy":[]}
{"id":"cth-17","title":"Render the call in the provider and patient views","notes":"Wire the two layers together and render two <video> elements. Local preview muted,\nremote unmuted. Thin: the view carries a peer ID from media to socket and one from socket to media,\nand that is all its media logic — a third concern means the seam is wrong.\n\nGate on state === ACTIVE. No Peer, no camera, no mic before that (§4.4) — a patient in the waiting\nroom must hold no media resources. ENDED tears it all down.\n\nProvider always calls, patient always answers (§4.4). Provider calls when it holds a patient peer\nID and media is not live.\n\nA denied camera permission or a peer error shows a message and leaves the session in ACTIVE with\nthe socket up and End session still working. No media fault touches the state machine.\n\nDone when: two tabs on one machine, admit, and each tab shows the other's camera with audio; the\ncamera light goes out on End session; and denying permission leaves the session usable.\n\nWired useMedia into both views via a shared VideoPanel; provider calls on remotePeerId + localPeerId, patient only answers. 27 view tests green, all three gates clean.\n\nRound 2: reversed the one-call-per-activation guard in useMedia to a liveness guard (d94c06b), pinned the provider re-call ordering across a rerender and dropped the invalid caption tracks for a biome rule opt-out (e56d45e). 172 tests green, all gates clean.\n\nReviewed and closed. Commits afd34b3..e56d45e. All twelve brief cases covered plus a thirteenth pinning the provider's re-call ordering across a rerender; 172 tests, biome and tsc -p packages/client all clean, no act warnings. No blocking findings left standing. Four out-of-scope findings carded: cth-18, cth-19, cth-20 and cth-21.","category":"client","tags":["media","ui"],"status":"done","rank":"zzzl","created":"2026-09-13T01:09:40.778Z","updated":"2026-09-13T07:54:19.913Z","refs":["packages/client/src/views/VideoPanel.tsx","packages/client/src/views/ProviderView.tsx","packages/client/src/views/PatientView.tsx"],"blockedBy":[]}
{"id":"cth-23","title":"A patient reconnect never restores presence.patient","notes":"Found by hand: after the patient leaves and reconnects, the provider still shows 'Patient: not connected'.\n\nCause. signaling.ts:143 guards the patient's connect on the CREATED state alone - 'if (role === patient && status === CREATED) registry.patientConnected(...)'. That is right for the first join, which is the WAITING transition, but it means a patient socket that connects in WAITING or ACTIVE touches presence at all. The registry has no transition for it either: patientConnected throws outside CREATED, and there is no patientReconnected. So presence.patient stays false for the rest of the session once anything has cleared it.\n\nThe disconnect half is missing too. signaling.ts:200 only removes the socket id from connectedSockets; it never clears presence.patient. Section 3 says a patient disconnect sets presence.patient false, sends session:state and writes a patient_disconnected event. So today the only thing that clears the flag is an explicit patient:leave, which is how the observed bug is reached.\n\nBoth event types already exist in the contract, unused: patient_disconnected and patient_reconnected in packages/shared/src/events.ts. Section 4.3 pins the semantics - 'A patient who rejoins the waiting room changes presence only' - so neither is a state transition and the grace timer stays out of it. Only a provider disconnect starts a grace period.\n\nFix shape: add patientDisconnected and patientReconnected to the registry, both presence-only and both emitting their event type; call them from the socket connect and disconnect handlers, keeping the CREATED-only patientConnected for the genuine first join.\n\nThis is the patient half of the reconnect story only. The provider half, the grace timer and DISCONNECTED_GRACE stay unbuilt and want their own plan.\n\nFixed in 565e857. Registry gained patientDisconnected and patientReconnected, both presence-only and both emitting transition with from equal to to so the event recorder writes a row; signaling calls them from the socket connect and disconnect handlers, keeping the CREATED-only patientConnected for the genuine first join. The disconnect path only clears presence when the disconnecting socket is still the registered one for its role, so a superseded socket cannot clear its replacement's presence. 16 new tests across sessionRegistry, signaling and eventRecorder; 188 total, lint and tsc clean on all three packages. Provider half of the reconnect story and the grace timer remain unbuilt.\n\nReviewed: diff a68135d..2939258 (this card's half is 565e857) checked against the card by a dispatched reviewer. Clean - 0 blocking, 0 out of scope. Findings at .work/cth-22-23-findings.md.","category":"server","tags":["bug","session","signaling"],"status":"done","rank":"zzzt","created":"2026-09-13T08:06:59.855Z","updated":"2026-09-13T08:31:24.285Z","refs":["packages/server/src/signaling/signaling.ts","packages/server/src/session/sessionRegistry.ts"],"blockedBy":[]}
{"id":"cth-22","title":"The PeerServer's ws aborts every Socket.IO websocket upgrade","notes":"Found by hand in Firefox: 'Firefox can't establish a connection to the server at ws://localhost:3001/socket.io/?EIO=4&transport=websocket&sid=...'. The sid in that URL means the polling handshake already succeeded, so the app keeps working on long-polling and only the upgrade fails. That is why every test passes and the console still shows an error.\n\nCause, confirmed in the installed sources. ExpressPeerServer builds its WebSocketServer as new WebSocketServer({ path: '/peerjs/peerjs', server }) (node_modules/peer/dist/module.mjs:259-265). Handed a 'server', ws attaches its own 'upgrade' listener, and ws@8.21.3 handleUpgrade does 'if (!this.shouldHandle(req)) { abortHandshake(socket, 400); }' (node_modules/ws/lib/websocket-server.js:277-278) - it destroys the socket on a path it does not own. Every Socket.IO upgrade takes that path.\n\nengine.io is the well-behaved half: its own upgrade listener ignores a path it does not own and only ends the socket after destroyUpgradeTimeout, 1000ms, and only if nothing was written (node_modules/engine.io/build/server.js:693-710). So reordering the two attachments does not fix it - it just moves which side gets destroyed.\n\nFix shape: pass createWebSocketServer to ExpressPeerServer, build the WebSocketServer with noServer: true, keep the returned instance, and add one server.on('upgrade') that calls peerWss.handleUpgrade only for the peer path. ws is hoisted at node_modules/ws but is a transitive dep - declare it in packages/server/package.json rather than importing it by luck.\n\nVerify in a browser, not only in a test: the check is that the Firefox console error is gone and the socket.io frame shows a websocket, not polling.\n\nParked before writing code: the fix lives in the composition root (architecture 6.2 puts the PeerServer there and gives it no handler), and index.ts has no test seam - importing it opens the database and listens. Needs a decision on where a testable seam goes before TDD can start.\n\nFixed in 2939258. Confirmed root cause in the installed sources: peer builds new WebSocketServer({path, server}) and ws@8.21.3 handleUpgrade calls abortHandshake(socket, 400) on a path it does not own, killing every Socket.IO upgrade. Fix builds the peer ws with noServer via the createWebSocketServer option and routes upgrades by path from one server.on('upgrade').\n\nPer the decision on the test seam: index.ts split into buildServer (wires everything onto one http.Server, returns it) plus a main that listens. No new module and no architecture change - section 6.2's 'the PeerServer is not a module and gets no handler' still holds. buildServer.test.ts builds on an ephemeral port and connects a real socket.io-client with transports: ['websocket'], which is the assertion that fails without the fix, plus a check that /peerjs/peerjs/id still serves.\n\n190 tests, lint and tsc clean. Smoke-tested the real server: boots, serves /peerjs/peerjs/id and POST /api/sessions. Still wants the browser check - the Firefox console error gone and the socket.io frame showing a websocket rather than polling.\n\nReviewed: diff a68135d..2939258 (this card's half is 2939258) checked against the card by a dispatched reviewer. Clean - 0 blocking, 0 out of scope. Findings at .work/cth-22-23-findings.md. Still unverified in a browser: the card's own check is that the Firefox console error is gone and the socket.io frame shows a websocket rather than polling. That is a by-hand check and no agent has done it.","category":"server","tags":["bug","peerjs","socket"],"status":"done","rank":"zzzx","created":"2026-09-13T08:06:48.170Z","updated":"2026-09-13T08:31:24.364Z","refs":["packages/server/src/index.ts"],"blockedBy":[]}
{"id":"cth-18","title":"A socket reconnect failure tears down a live call","notes":"Found by the cth-17 review. useSession maps both socket 'error' and 'connect_error' to { status: 'error' }, and socket.io-client emits connect_error for failed RECONNECTION attempts, not only the first connect. So a server restart or a few seconds of network loss during a live call flips connection.status away from 'connected', which flips the views' active flag false, which stops every track and destroys the peer, and replaces the view with 'Session unavailable'.\n\nThat breaks the architecture's section 3 property that the two connections fail independently: a Socket.IO reconnect must not tear down or re-create the call.\n\nFix shape: latch the last known session state so the connected branch survives a transient socket fault, or stop treating a post-connect connect_error as terminal. A handshake refusal on the FIRST connect must stay terminal - that is deliberate and tested.\n\nNeeds a browser to verify: kill and restart the server mid-call and watch whether the camera light goes out.\n\nImplemented: connect_error after first connect is now ignored, tracked via a per-effect-run local flag set from socket.on('connect'). Added tests to useSession.test.ts.\n\nFixed in e41a6ff. A connect_error is terminal only before the socket has ever connected; once connected it is ignored and the last session:state is kept, so a server restart mid-call no longer flips active false. The server-sent error event stays terminal. Reviewed as part of the batch diff 2939258..d5905e5 by a dispatched reviewer: clean, 0 blocking, 0 out of scope (.work/cth-18-19-21-findings.md). Still wants the by-hand browser check the card names: kill and restart the server mid-call and watch whether the camera light goes out.","category":"client","tags":["bug","media","socket"],"status":"done","rank":"zzzz","created":"2026-09-13T03:09:51.001Z","updated":"2026-09-13T08:33:09.352Z","refs":["e41a6ff"],"blockedBy":[]}
{"id":"cth-19","title":"The patient keeps the camera and the call after Leave","notes":"Found by the cth-17 review. registry.patientLeft only flips presence.patient; the session stays ACTIVE. So the patient view's active flag stays true after Leave: the camera light stays on, the call stays up, the Leave button stays rendered, and the only visible effect is the provider's 'Patient: not connected' line.\n\nNot a violation of any card's literal spec, but it contradicts the invariant the media slice was built around - no camera outside a live consultation - and a user notices it immediately.\n\nTwo candidate fixes, and the choice is a product decision: derive the patient's active flag as state.state === 'ACTIVE' && state.presence.patient, which keeps the 'session state alone' rule intact; or give the registry a transition that takes the session out of ACTIVE when the patient leaves, which is a state machine change and needs section 3 updated.\n\nProduct decision made by the coordinator rather than deferred: the patient's active flag is derived as state === ACTIVE && presence.patient, keeping the 'session state alone' rule and leaving the server state machine untouched. Cost if wrong: if the product wants Leave to actually end the consultation (taking the session out of ACTIVE), this is a client-only patch over a server-side gap, and the provider would still see a live ACTIVE session with no patient. That would need section 3 updated and a registry transition.\n\nImplemented: PatientView's active flag now also requires connection.state.presence.patient. Added tests to PatientView.test.tsx.\n\nFixed in ccdd3dc. PatientView's active flag now also requires connection.state.presence.patient, per the decision noted above. Server state machine untouched. Reviewed as part of the batch diff 2939258..d5905e5: clean, 0 blocking, 0 out of scope.","category":"client","tags":["bug","media"],"status":"done","rank":"zzzzV","created":"2026-09-13T03:10:01.199Z","updated":"2026-09-13T08:33:09.429Z","refs":["ccdd3dc"],"blockedBy":[]}
{"id":"cth-21","title":"A stale call-ended message survives a reconnect","notes":"Found by the cth-17 review, round 2, and not previously carded.\n\nuseMedia's trackCall sets error to CALL_ENDED_ERROR on a call's 'close' and nothing clears error when a new call is tracked - the only clear is on teardown, when 'active' goes false. Since d94c06b a closed call no longer ends the activation, so the reconnect path that commit opened up lands the user on a live remote video with 'The video call ended.' rendered underneath it.\n\nFix shape: clear error at the top of trackCall, when a new call is tracked. That is a behaviour change in the media layer, which is why cth-17's round 2 did not take it.\n\nNote that error is a single slot shared by permission faults, peer faults and call faults, so check that clearing it on a new call cannot swallow a live getUserMedia or peer error before widening the fix.\n\nImplemented: trackCall now clears error as its first statement. Added tests to useMedia.test.ts.\n\nFixed in d5905e5. trackCall clears the shared error slot as its first statement, so a re-call no longer renders the stale 'The video call ended.' under a live remote video. The clear is confined to trackCall - it runs only after capture has resolved and only when a call exists, so it cannot swallow a live getUserMedia fault. Reviewed as part of the batch diff 2939258..d5905e5: clean, 0 blocking, 0 out of scope.","category":"client","tags":["bug","media"],"status":"done","rank":"zzzzl","created":"2026-09-13T07:54:13.812Z","updated":"2026-09-13T08:33:09.508Z","refs":["d5905e5"],"blockedBy":[]}
{"id":"cth-24","title":"Leave does not end the patient's session, and a refresh rejoins it","notes":"Found by hand after cth-19. The patient clicks Leave: the camera does stop now, but the view still reads 'State: ACTIVE', and refreshing the page puts the patient straight back into the call.\n\nCause. cth-19 was fixed on the client only - PatientView derives active from state === ACTIVE && presence.patient - which is what stops the camera. The server's registry.patientLeft still only flips presence.patient and leaves the session in ACTIVE, so the state line renders ACTIVE because it is ACTIVE. The refresh then opens a new patient socket, which since 565e857 calls patientReconnected, which sets presence.patient back to true - so the client's active flag goes true again and the call resumes. From the server's point of view a deliberate Leave and a network drop are the same event, and a rejoin is the recovery for both.\n\nThis is the cost recorded on the cth-19 decision note, now observed. Fixing it means Leave stops being presence-only: a patient leave needs a state transition out of ACTIVE, which is a state machine change and needs architecture section 3 updated before any code is written. It also needs the product answer on what a rejoin means after a deliberate Leave - a new consultation, a closed session, or a return to WAITING for the provider to re-admit.\n\nDo not fix this on the client by hiding the state line or blocking the rejoin; that hides a server-side gap behind a view.\n\nFixed: patientLeft() in sessionRegistry.ts now transitions ACTIVE -> WAITING (previously same-state, presence-only), so a deliberate Leave stops reading as ACTIVE and a refresh rejoin lands in WAITING requiring the provider to re-admit, per the 2026-09-13 product decision. Leave from WAITING (pre-admit) still stays WAITING - no change needed there. Updated architecture §3 (state diagram + bullet) and §4.3 (patient:leave row) to document the transition. Updated sessionRegistry.test.ts and signaling.test.ts to assert the new behavior (previous tests encoded the bug).","category":"server","tags":["bug","session"],"status":"done","rank":"zzzzt","created":"2026-09-13T08:50:20.603Z","updated":"2026-09-13T09:01:00.604Z","refs":["packages/server/src/session/sessionRegistry.ts","packages/server/src/signaling/signaling.ts","docs/architecture.md"],"blockedBy":[]}
{"id":"cth-25","title":"Move a dropped provider into DISCONNECTED_GRACE and time it out","notes":"Plan 0003, Approach paragraphs 2 and 3. Add providerDisconnected to the registry: in WAITING or ACTIVE it stores the current status on the entry, moves to DISCONNECTED_GRACE, sets presence.provider false, emits a provider_disconnected transition and schedules the timeout. In CREATED it only clears presence (the known limit in architecture.md section 3); in ENDED it does nothing; a second call while already in DISCONNECTED_GRACE is a no-op and must not restart the timer.\n\nThe timeout fires two transitions, not one: session_timed_out with from equal to to (the same trick setPatientPresence at sessionRegistry.ts:173 already uses, because the event recorder maps one transition to exactly one recordEvent), then the ordinary session_ended to ENDED with endedReason timeout. If that redundant DISCONNECTED_GRACE broadcast turns out to matter, re-read the Approach section before improvising.\n\nThe grace period is a constructor option defaulting to 10 minutes, defined in this module only, so the test can use milliseconds. unref the timer or the vitest process hangs on a pending session. endSession must clear a pending timer.\n\nDone when: a registry with a short grace period, driven with no socket and no database, goes ACTIVE -> DISCONNECTED_GRACE -> ENDED on its own and emits provider_disconnected, session_timed_out and session_ended in that order.\n\nCommits cba4e1d..23b2f48 (batched with cth-26, cth-27). Review clean, 0 blocking. Reviewer flagged a minor coverage gap (endSession-during-grace timer cancellation untested) -> logged as cth-30.","category":"server","tags":[],"status":"done","rank":"zzzzx","created":"2026-09-13T09:06:16.178Z","updated":"2026-09-13T09:23:20.546Z","refs":["docs/plans/0003-grace-timer-and-disconnect.md","packages/server/src/session/sessionRegistry.ts","packages/server/src/eventRecorder/eventRecorder.ts"],"blockedBy":[]}
{"id":"cth-26","title":"Restore the pre-drop state on a provider reconnect in time","notes":"Plan 0003, Spec paragraph 3. providerConnected at sessionRegistry.ts:86 currently returns early when presence.provider is already true and otherwise emits presence only, so it can never emit a transition. Give it a DISCONNECTED_GRACE branch: cancel the pending timer, restore the status remembered by cth-25, set presence.provider true, and emit a provider_reconnected transition whose 'to' is the restored status. Every other state keeps today's presence-only behaviour unchanged - the first provider connect in CREATED must not start emitting transitions.\n\nNo media rule changes here. Returning to ACTIVE is enough; architecture.md section 4.4 already covers rebuilding the call.\n\nDone when: a registry driven ACTIVE -> DISCONNECTED_GRACE -> providerConnected lands back in ACTIVE with presence.provider true, writes provider_reconnected, and the grace timer never fires afterwards.\n\nCommits cba4e1d..23b2f48 (batched with cth-25, cth-27). Review clean, 0 blocking.","category":"server","tags":[],"status":"done","rank":"zzzzz","created":"2026-09-13T09:06:23.614Z","updated":"2026-09-13T09:23:20.625Z","refs":["docs/plans/0003-grace-timer-and-disconnect.md","packages/server/src/session/sessionRegistry.ts:86"],"blockedBy":["cth-25"]}
{"id":"cth-27","title":"Demote the remembered state when the patient leaves during grace","notes":"Plan 0003, Spec paragraph 4. patientLeft at sessionRegistry.ts:142 demotes ACTIVE to WAITING but knows nothing about the state remembered for a provider reconnect. In DISCONNECTED_GRACE it currently clears presence.patient and leaves the remembered ACTIVE intact, so a provider reconnecting afterwards would land in ACTIVE with no patient present - a state that contradicts its own presence flags.\n\nMake patientLeft rewrite the remembered state to WAITING when it is ACTIVE. The visible status stays DISCONNECTED_GRACE either way; only what the reconnect restores changes.\n\nDone when: ACTIVE -> provider drops -> patient:leave -> provider reconnects lands in WAITING with presence.patient false, and the provider has to admit again.\n\nCommits cba4e1d..23b2f48 (batched with cth-25, cth-26). Review clean, 0 blocking.","category":"server","tags":[],"status":"done","rank":"zzzzzV","created":"2026-09-13T09:06:29.724Z","updated":"2026-09-13T09:23:20.702Z","refs":["docs/plans/0003-grace-timer-and-disconnect.md","packages/server/src/session/sessionRegistry.ts:142"],"blockedBy":["cth-26"]}
{"id":"cth-28","title":"Report the provider's disconnect and reconnect from Signaling","notes":"Plan 0003, Approach paragraph 4. The disconnect handler at signaling.ts:207-222 acts only 'if (role === \"patient\")', so a provider closing its tab changes nothing. Add the provider branch: call registry.providerDisconnected(sessionId) when the registry still holds the session.\n\nThe identity check at signaling.ts:210 - which returns early when connectedSockets no longer maps the role to this socket id - must guard the new branch too. Without it a superseded socket's late disconnect starts a grace period for a session its replacement is already holding. That exact bug was cth-23 on the patient side.\n\nThe connection handler at signaling.ts:152 already calls registry.providerConnected unconditionally, so cth-26 makes the reconnect path work with no change here. Confirm that rather than adding a second call.\n\nDone when: an integration test where the provider socket disconnects sees the patient socket receive session:state with DISCONNECTED_GRACE and presence.provider false, and a fresh provider socket on the same key brings ACTIVE back.\n\nCommit 97ab1cf. Review clean, 0 blocking. Implementer also fixed a pre-existing test assertion that the new wiring made stale (provider drop now goes to DISCONNECTED_GRACE, not WAITING) - reviewer confirmed the fix preserved the test's original intent (presence.patient staying true).","category":"server","tags":[],"status":"done","rank":"zzzzzl","created":"2026-09-13T09:06:37.892Z","updated":"2026-09-13T09:28:16.958Z","refs":["docs/plans/0003-grace-timer-and-disconnect.md","packages/server/src/signaling/signaling.ts:207-222","packages/server/src/signaling/signaling.ts:152"],"blockedBy":["cth-26"]}
{"id":"cth-29","title":"Show the ended screen when a reconnect is refused after a timeout","notes":"Plan 0003, Spec last paragraph. The connect_error handler at useSession.ts:73-82 returns early whenever the socket has ever connected. That was cth-18: socket.io-client fires connect_error for failed reconnection attempts too, and a transient one must not tear a live call down. But a handshake refusal after the grace period elapses arrives through the same callback and is swallowed, and socket.io-client stops retrying after a middleware refusal - so the provider's tab sits on a stale ACTIVE with no socket and no error, which architecture.md section 3 says must not happen.\n\nThe discriminator is the payload: only a middleware refusal attaches err.data (the ErrorPayload). Narrow the early return to the no-payload case. Do not key off the code value - session_ended, unknown_key and role_already_connected should all be terminal.\n\nKeep the cth-18 regression test passing; it is the reason the early return exists.\n\nDone when: a socket that has connected, then receives connect_error carrying { code: 'session_ended' }, leaves useSession in status 'error' and the view renders the ended message.\n\nCommit 293cdd3. Review clean, 0 blocking. Corrected a pre-existing cth-18 test that used a payload-carrying connect_error while asserting it was ignored - that assertion contradicted this task's new payload-based discriminator, so the test now uses a payload-less error to keep testing the real transient-failure case.","category":"client","tags":[],"status":"done","rank":"zzzzzt","created":"2026-09-13T09:06:46.488Z","updated":"2026-09-13T09:31:04.627Z","refs":["docs/plans/0003-grace-timer-and-disconnect.md","packages/client/src/session/useSession.ts:73-82"],"blockedBy":[]}
{"id":"cth-31","title":"Add Services.getSessionEvents(providerKey)","notes":"Plan 0004, Spec paragraphs 1-4. architecture.md section 6.2 lists getSessionEvents(providerKey) as the fourth Services function; only createSession, resolveKey and endInterruptedSessions exist.\n\nIt does its own lookup with repository.getSessionByKey(key) and compares session.providerKey === key. Do not route it through resolveKey: that returns a derived role but not the keys, and section 6.2 says this path calls persistence directly and never calls Session. Return Event[] | undefined so the transport picks the status code and the service holds no HTTP knowledge.\n\nundefined for both an unknown key and a patient key - the caller cannot tell the two apart, which is the point (section 4.5). An ENDED session still returns its rows; the record is why the endpoint exists.\n\nRows come back from repository.getEvents already in id order with data JSON-parsed, so no sorting or parsing here.\n\nDone when: sessionService.test.ts covers a provider key with rows in order, a patient key, an unknown key, and a session with an empty log.\n\nCommit 247b526 (293cdd3..247b526). Review clean, 0 blocking, 0 out-of-scope.","category":"server","tags":["services"],"status":"done","rank":"zzzzzx","created":"2026-09-13T10:48:54.228Z","updated":"2026-09-13T10:56:09.633Z","refs":["docs/plans/0004-session-event-log-endpoint.md","packages/server/src/services/sessionService.ts:12-16","packages/server/src/persistence/sessionRepository.ts:101-107"],"blockedBy":[]}
{"id":"cth-32","title":"Serve GET /api/sessions/:providerKey/events","notes":"Plan 0004. Third row of the architecture.md section 4.2 table. Today the request falls through to Express's HTML 404 instead of the { code, message } body of section 4.5, because GET /api/sessions/:key matches a single path segment only.\n\nReturns { events: Event[] } from GetSessionEventsResponseSchema, oldest row first. Parse the path parameter with GetSessionEventsParamsSchema from shared like the other two routes - no handler reads a raw parameter. undefined from services.getSessionEvents maps to 404 unknown_key with the same message used by GET /api/sessions/:key, for a patient key and an unknown key alike. An empty log is 200 with { events: [] }.\n\nOrdering risk: :key must not shadow :providerKey/events. Express matches segments rather than prefixes so declaration order should not matter - the test that a real provider key returns rows and not { role, status } is what proves it.\n\nDone when: http.test.ts covers a provider key, a patient key, an unknown key, and an empty log against a stub SessionService.\n\nCommit 47b1a1e (247b526..47b1a1e). Review clean, 0 blocking, 0 out-of-scope.","category":"server","tags":["http"],"status":"done","rank":"zzzzzz","created":"2026-09-13T10:49:02.994Z","updated":"2026-09-13T10:58:34.103Z","refs":["docs/plans/0004-session-event-log-endpoint.md","packages/server/src/http/http.ts","packages/shared/src/http.ts:31-41"],"blockedBy":["cth-31"]}
{"id":"cth-33","title":"Add getSessionEvents to the client HttpClient","notes":"Plan 0004, Spec last paragraph. architecture.md section 6.3: the session layer wraps the HTTP calls and no view calls fetch.\n\ngetSessionEvents(providerKey): Promise<GetSessionEventsResponse>, same shape as the existing getSession - encodeURIComponent on the key, throw HttpError(await parseErrorBody(res)) on a non-OK response, parse the body with GetSessionEventsResponseSchema.\n\nNothing consumes it yet. It ships as the seam the provider-history UI builds on; that UI is out of scope in plan 0004 because it needs a decision about where the record sits on the ended screen.\n\nDone when: httpClient.test.ts covers a 200 with rows and a 404 unknown_key that throws HttpError carrying the payload, against a stubbed fetch.\n\nCommit 3734595 (47b1a1e..3734595). Review clean, 0 blocking, 0 out-of-scope. Plan 0004 complete: service, route, and client all shipped.","category":"client","tags":["http"],"status":"done","rank":"zzzzzzV","created":"2026-09-13T10:49:10.854Z","updated":"2026-09-13T11:00:19.959Z","refs":["docs/plans/0004-session-event-log-endpoint.md","packages/client/src/session/httpClient.ts:20-23","packages/client/src/session/httpClient.test.ts"],"blockedBy":["cth-32"]}
{"id":"cth-34","title":"Amend the architecture for a styling layer","notes":"Approach step 1, and it blocks everything else in plan 0005: §1 says \"no design system\" and §2.1 says a dependency must replace code the document describes. Tailwind violates both as written.\n\nThree edits. §1: keep the point that visual appearance is not what the brief assesses, but stop forbidding a styling layer. §2.1: add a Tailwind row naming what it replaces — the hand-written stylesheet and the class-naming convention a project with no design system has to invent and keep consistent by review. §6.3: one line saying where client styles live.\n\nDone when: docs/architecture.md no longer contradicts a Tailwind dependency in packages/client, and §2.1's table has a row for it. No code changes in this task.\n\nEdited directly (docs-only, no code, no tests to review). Commit 0c9c229. Amended §1, §2.1 table, §6.3 per brief.","category":"docs","tags":["architecture","ui"],"status":"done","rank":"zzzzzzl","created":"2026-09-14T05:35:59.769Z","updated":"2026-09-14T05:39:57.385Z","refs":["docs/plans/0005-call-screen-layout.md","docs/architecture.md"],"blockedBy":[]}
{"id":"cth-35","title":"Install Tailwind in the client with no view changes","notes":"Approach step 2. Deliberately alone: the view tests run under jsdom via the ROOT vitest.config.ts, which does not use the client's Vite plugin chain, and a CSS import in the module graph is the usual way that breaks. Isolating it means every later layout task starts from a green baseline.\n\nTailwind v4: `tailwindcss` + `@tailwindcss/vite` in packages/client, the plugin added to vite.config.ts (which currently holds only the react plugin), one stylesheet with `@import \"tailwindcss\"`, imported from main.tsx. No .tsx markup changes.\n\nIf the plugin cannot coexist with the root Vitest config, fall back to a plain stylesheet with CSS custom properties and note it on this task — the rest of plan 0005 is unaffected either way.\n\nDone when: `npm test` passes unchanged, `npm run build -w @coviu/client` succeeds, and a Tailwind utility class added by hand to any element visibly applies in `npm run dev`.\n\nCommit fd7bc73 (0c9c229..fd7bc73). Tailwind plugin coexists with root vitest.config.ts, no fallback needed. Review clean, 0 blocking.","category":"client","tags":["tooling","ui"],"status":"done","rank":"zzzzzzt","created":"2026-09-14T05:36:10.121Z","updated":"2026-09-14T05:42:28.202Z","refs":["docs/plans/0005-call-screen-layout.md","packages/client/vite.config.ts","packages/client/src/main.tsx","vitest.config.ts"],"blockedBy":["cth-34"]}
{"id":"cth-36","title":"Rewrite VideoPanel as a filled video stage with a local inset","notes":"Approach step 3. Comes before either view moves its controls, because both views consume this — doing it first leaves each view's own change as only \"move the buttons into a bar\".\n\nDelete VIDEO_WIDTH (VideoPanel.tsx:11) and the width attributes at :44 and :54. Remote video fills the stage with object-fit: cover, centred, aspect ratio kept, cropping not letterboxing. Local video becomes a small PiP tile overlapping the remote feed.\n\nGuarded before you start: VideoPanel.test.tsx already pins the two accessible names (Remote video / Local video), the muted asymmetry, srcObject assignment and null teardown, and role=alert. Keep all of it — useSrcObject at :16-27 is unchanged, this is markup and classes.\n\nNew behaviour needing new tests first: a tile whose stream is null renders the role name plus a short reason line, not an empty video element. Cases: remote null (waiting), local null (capture not resolved).\n\nThe media error keeps role=alert and overlays the stage rather than displacing it.\n\nOpen question to settle before starting: which corner the local inset sits in. Plan assumes bottom-right.\n\nDone when: with both streams null the panel shows two labelled placeholders with reason text, and with both set the remote element fills its container while the local one renders as a small overlapping tile.","category":"client","tags":["media","ui"],"status":"done","rank":"zzzzzzx","created":"2026-09-14T05:36:23.620Z","updated":"2026-09-14T05:46:37.540Z","refs":["docs/plans/0005-call-screen-layout.md","packages/client/src/views/VideoPanel.tsx","packages/client/src/views/VideoPanel.test.tsx"],"blockedBy":["cth-35"]}
{"id":"cth-30","title":"Test that endSession during DISCONNECTED_GRACE cancels the pending grace timer","notes":"Found by the cth-25/26/27 review. endSession clears the pending timer correctly but no test drives endSession during DISCONNECTED_GRACE and then waits past the original grace period to confirm no session_timed_out/session_ended fires afterward. No functional risk today (independently guarded by the DISCONNECTED_GRACE status check in the timeout callback) - pure coverage gap.\n\nAdded test confirming endSession clears the grace timer: after providerDisconnected -> endSession, waiting past the original grace period fires no session_timed_out and no second transition. Confirms no functional risk, closes the coverage gap.","category":"server","tags":[],"status":"done","rank":"zzzzzzz","created":"2026-09-13T09:23:14.878Z","updated":"2026-09-14T05:47:03.568Z","refs":["packages/server/src/session/sessionRegistry.ts"],"blockedBy":[]}
{"id":"cth-37","title":"Extract a bottom control bar component","notes":"Approach step 4. §6.3 is explicit that the two views stay separate components and that shared BEHAVIOUR is what gets factored out, not shared RENDERING — so this takes status content and buttons as children/slots and never takes a role prop to branch on. It owns the fixed-bottom full-width layout and the slot arrangement, nothing else.\n\nSlots: a status region on one side (currently the \"State: X\" and \"Patient: connected\" paragraphs at ProviderView.tsx:64-65) and an actions region for the buttons.\n\nAlso needs the viewport shell this bar sits in: video area takes the height the bar does not, page never scrolls at or above 320x480.\n\nDone when: rendering the component with arbitrary status and button children puts them in a full-width bar pinned to the bottom, and the sibling video area consumes exactly the remaining viewport height with no page scrollbar.\n\nImplemented as two components rather than one: ControlBar (status/actions slots, w-full shrink-0, no role prop — plain rendering) and CallScreenShell (h-screen flex flex-col overflow-hidden; video area is flex-1 min-h-0 overflow-hidden, a flex sibling of the bar rather than position:fixed, so the video area's height is exactly whatever the bar doesn't take with no manual offset to keep in sync). Neither view is wired up yet — that's cth-38/cth-39. All 250 client+server tests and the client build pass unchanged.","category":"client","tags":["ui"],"status":"done","rank":"zzzzzzzV","created":"2026-09-14T05:36:29.174Z","updated":"2026-09-14T05:50:17.560Z","refs":["docs/plans/0005-call-screen-layout.md","packages/client/src/views/ControlBar.tsx","packages/client/src/views/CallScreenShell.tsx"],"blockedBy":["cth-35"]}
{"id":"cth-38","title":"Move PatientView onto the call screen frame","notes":"Approach step 5, patient first: one button and no history section, so it exercises the whole frame — shell, video stage, bar — with the least in the way. ProviderView copies whatever shape lands here.\n\nDrop the <h1>Patient</h1> and the \"State: X\" paragraph from the top; the state readout moves into the bar's status slot and Leave into its actions slot. Leave still shows in every state but ENDED — no condition changes, only placement.\n\nENDED replaces the frame rather than filling it: a centred message, no video, no bar.\n\nThe three non-call returns (resolving at :36-38, error at :40-47, and ENDED) each render as centred content on the same background. Keep role=alert on the error branch.\n\nThe 215 lines of PatientView.test.tsx query by role and text, so most should pass untouched. A break is either the h1 removal (a spec change) or a defect.\n\nDone when: at 1440x900 in ACTIVE the video fills the screen above a bottom bar holding the state and Leave, the page has no scrollbar, and no h1 renders on the call screen.\n\nPatientView now composes CallScreenShell + ControlBar: video in the shell's video slot, state readout in the bar's status slot, Leave in actions. ENDED short-circuits to a centred 'The session has ended.' message with no video/bar. Dropped the top-level h1 and the standalone State paragraph per spec. Loading and error branches now render inside the same centred dark background (CenteredScreen, local to this file) instead of unstyled top-of-page text.\n\nThis also fixes the bug reported live: VideoPanel's cth-36 rewrite assumes a parent with a defined height (w-full h-full), but PatientView/ProviderView were still wrapping it in a bare div with no height, so the video area collapsed to 0px after admitting a patient. CallScreenShell supplies that height via flex-1 min-h-0. ProviderView (cth-39) still has the bug until it's wired up the same way.","category":"client","tags":["ui"],"status":"done","rank":"zzzzzzzl","created":"2026-09-14T05:36:42.479Z","updated":"2026-09-14T06:01:42.033Z","refs":["docs/plans/0005-call-screen-layout.md","packages/client/src/views/PatientView.tsx:51-67","packages/client/src/views/PatientView.test.tsx"],"blockedBy":["cth-36","cth-37"]}
{"id":"cth-39","title":"Move ProviderView onto the call screen frame and restyle the ended screen","notes":"Approach step 5 continued. Follows PatientView because it adds the two cases that view does not have: two buttons, and an ENDED screen that holds content rather than a message.\n\nBar status slot takes both readouts currently at ProviderView.tsx:64-65 — the state and \"Patient: connected|not connected\". Actions slot takes Admit patient (WAITING only) and End session (every state but ENDED). Conditions unchanged. End session must read as the destructive action and look distinct from Admit patient.\n\nDrop the <h1>Provider</h1>.\n\nENDED replaces the frame: no video, no bar, a centred panel holding the Session history section (:76-91). The history list scrolls INSIDE the panel — it is the one unbounded thing on screen and must not grow the page. Its three branches (loading, error alert, loaded list) all live in the panel.\n\n272 lines of ProviderView.test.tsx query by role and text; a break is either the h1 removal or a defect.\n\nDone when: WAITING shows both buttons in the bottom bar with the presence readout beside them, ACTIVE shows only End session there, and ENDED shows a centred history panel that scrolls internally with a long event list while the page does not.\n\nImplemented: ProviderView now uses CallScreenShell + ControlBar for WAITING/ACTIVE, dropping the h1. Bar status slot shows both State and Patient presence text; actions slot has Admit patient (WAITING only) and End session (styled red/destructive) for every non-ENDED state. ENDED replaces the frame with a centred CenteredScreen holding a SessionHistory panel (max-h-[80vh], internal overflow-y-auto on the list) so the page itself never scrolls even with a long event list. Added 4 new tests to ProviderView.test.tsx covering h1 removal, video+bar coexistence, WAITING dual-button+presence, and ENDED video/bar absence. All 20 ProviderView tests and tsc pass.","category":"client","tags":["ui"],"status":"done","rank":"zzzzzzzt","created":"2026-09-14T05:36:54.828Z","updated":"2026-09-14T06:06:05.705Z","refs":["docs/plans/0005-call-screen-layout.md","packages/client/src/views/ProviderView.tsx:61-98","packages/client/src/views/ProviderView.test.tsx"],"blockedBy":["cth-38"]}
{"id":"cth-40","title":"Restyle HomeView as a centred card","notes":"Approach step 6, last and independent: it shares only the background and the type scale with the call screens, and nothing else in plan 0005 depends on it. This is the task to drop if the work stops early.\n\nNot a call screen — no control bar. A centred card holding the heading, the Create session button, and once created the provider and patient links. The error branch at :37 keeps role=alert.\n\nThe two links are the thing a user copies out, so make them readable and easy to select rather than default-blue inline anchors.\n\nDone when: the home page renders as a single centred card on the same background as the call screens, at 1440x900 and at 400px wide, with no page scrollbar in the idle state.\n\nImplemented: HomeView is now a full-height flex-centred wrapper (bg-neutral-950, matching the call screens) holding a card (bg-neutral-900, rounded, max-w-md). Create session button and, once created, the provider/patient links are inside the card. Links are styled as readable blocks (bg-neutral-800, break-all, no default blue-underline look) rather than inline anchors, with a small label above each. Added 2 new tests to HomeView.test.tsx: card layout classes, and links carry non-empty custom styling. All 4 HomeView tests, and the full 56-test client views suite, pass; tsc clean.","category":"client","tags":["ui"],"status":"done","rank":"zzzzzzzx","created":"2026-09-14T05:36:59.126Z","updated":"2026-09-14T06:06:56.389Z","refs":["docs/plans/0005-call-screen-layout.md","packages/client/src/views/HomeView.tsx"],"blockedBy":["cth-35"]}
